# ─────────────────────────────────────────────────────────────────────────────
# .env.example — copy to .env for local dev
# NEVER commit .env to git  (it's in .gitignore)
#
# In Azure: set these as App Service → Configuration → Application Settings
# ─────────────────────────────────────────────────────────────────────────────

# ── VedicAstro API ─────────────────────────────────────────────────────────
VEDIC_ASTRO_API_KEY=your-key-here

# ── Redis ──────────────────────────────────────────────────────────────────
# Local dev:
#   REDIS_URL=redis://localhost:6379
# Production (Redis Cloud / Redis Labs):
#   REDIS_URL=redis://default:<password>@<host>:<port>
REDIS_URL=redis://localhost:6379

# ── MongoDB Atlas ──────────────────────────────────────────────────────────
MONGODB_ENABLED=True
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/myAppDB
MONGODB_DB_NAME=myAppDB

# ── Rules file (relative to WORKDIR /app inside container) ────────────────
RULES_FILE=rules/BPHS_Level2_200_Rules.json

# ── Azure App Service ─────────────────────────────────────────────────────
# Set in App Service Configuration (not here):
#   WEBSITES_PORT=8000
